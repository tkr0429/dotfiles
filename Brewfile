# Make sure using latest Homebrew
update  || true
upgrade || true

# Add Repository
tap caskroom/cask              || true
tap homebrew/apache            || true
tap homebrew/binary            || true
tap homebrew/completions       || true
tap homebrew/dupes             || true
tap homebrew/versions          || true
tap rickard-von-essen/formulae || true

# Package
install ansible            || true
install autoconf           || true
install automake           || true
install bash-completion    || true
install boot2docker        || true
install coreutils          || true
install docker             || true
install enscript           || true
install fontconfig         || true
install fontforge          || true
install freetype           || true
install gawk               || true
install gdbm               || true
install gettext            || true
install git                || true
install htop-osx           || true
install ipcalc             || true
install iperf              || true
install jpeg               || true
install libpng             || true
install libtiff            || true
install libtool            || true
install libyaml            || true
install mod_wsgi           || true
install nkf                || true
install nmap               || true
install openssl            || true
install packer             || true
install perl-build         || true
install pkg-config         || true
install plenv              || true
install prl-utils          || true
install pwgen              || true
install pyenv              || true
install pyenv-virtualenv   || true
install rbenv              || true
install readline           || true
install ruby-build         || true
install screen             || true
install sipcalc            || true
install sqlite             || true
install sshpass            || true
install tree               || true
install vagrant-completion || true
install watch              || true
install wget               || true
install xvid               || true
install xz                 || true

# cask package 
install brew-cask        || true
cask install boot2docker || true
cask install coteditor   || true
cask install iterm2      || true
cask install macvim      || true
cask install perian      || true
cask install vagrant     || true

# Remove outdated bersions
cleanup
